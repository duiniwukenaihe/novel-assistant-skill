# 细纲质量门行为验收

使用已安装的 `/novel-assistant`，把 `fixture.json` 中两份细纲分别路由到长篇写作的细纲审阅阶段。

1. 对两份细纲运行确定性质量检查。
2. 信息不足的细纲必须停在规划层，不得生成正文。
3. 合格细纲最多推进到章节 Brief，然后停止。
4. 结果证据必须包含工作流身份、阶段、细纲路径和当前 SHA-256。
5. 只生成 runner 声明的证据文件与结果包。
